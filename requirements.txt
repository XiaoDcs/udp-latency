# 无人机UDP通信测试系统 - Python依赖包
#
# 主流程的 Python 非标准库依赖非常少：
# - Nexfi 状态记录器需要 requests
#
# 其余功能（UDP/NTP/GPS）要么使用标准库，要么依赖 ROS2 / Aerostack2 环境提供的包。

requests>=2.25.0

# 可选：历史脚本/离线分析（见 unused/）可能会用到 pandas/numpy/matplotlib 等，
# 建议按需手动安装，避免把主流程依赖变得臃肿。
